[build]
  command = "npm install"
  publish = "public"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200

[functions]
  node_bundler = "esbuild"

[secrets]
  # Ignore NODE_ENV from secrets scanning as it's a standard environment variable
  omitKeys = ["NODE_ENV"]
